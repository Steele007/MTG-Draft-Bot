class CardPool {

  #sortedPool;

  constructor(cardPool){

    this.#sortedPool = new Map;
    this.#sortedPool.set("land", []);
    this.#sortedPool.set("common", []);
    this.#sortedPool.set("uncommon", []);
    this.#sortedPool.set("rare", []);
    this.#sortedPool.set("uncommonMythic", []);
    
  }

  getLand(){

    
  }

  getCommon(){


    
  }

  getUncommon(){

    
  }
  getRare(){

    
  }
  getMythic(){

    
  }
}